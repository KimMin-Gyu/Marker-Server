{
  "name": "logo",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "docker": "docker build -t logo-api .",
    "docker:run": "docker run -p 3000:3000 logo-api"
  },
  "dependencies": {
    "canvas": "^3.1.0",
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "module": "src/index.js"
}
